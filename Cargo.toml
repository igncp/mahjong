[workspace]
members = [
    "tui_client",
    "mahjong_core",
    "service",
    "service_contracts",
    "scripts"
]

[profile.release]
opt-level = 'z'
codegen-units = 1
lto = true
strip = true
panic = "abort"
