import { StyleSheet } from "react-native";

export const styles = StyleSheet.create({
  input: {
    borderColor: "gray",
    borderWidth: 1,
    paddingLeft: 10,
  },
  introText: {
    fontSize: 15,
  },
  title: {
    alignItems: "center",
    display: "flex",
    height: 80,
    justifyContent: "center",
  },
  titleText: {
    fontSize: 30,
  },
  wrapper: {
    gap: 20,
    padding: 10,
  },
});
